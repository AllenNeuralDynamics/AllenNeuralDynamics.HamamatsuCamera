---
uid: frame_bundles
---
# FrameBundles

**Description**  
The `FrameBundles` node converts `IFrameContainer` objects into `FrameBundle` objects by filtering out any single `Frame` instances.

**Output**  
- `FrameBundle`