---
uid: frames
---
# Frames

**Description**  
The `Frames` node converts `IFrameContainer` objects into `Frame` objects by filtering out any `FrameBundle` instances. This provides direct access to an `IplImage` for every image acquired from the camera.

**Output**  
- `Frame`